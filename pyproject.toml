[project]
name = "pesu-oauth2"
version = "0.0.0"
description = "Unofficial PESU Academy OAuth2 / OpenID Connect authorization server"
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }
authors = [{ name = "pesu-dev" }]
dependencies = [
    "cryptography>=50.0.1",
    "fastapi>=0.115.0",
    "httpx>=0.28.1",
    "itsdangerous>=2.2.0",
    "jinja2>=3.1.6",
    "nanoid>=2.0.0",
    "pyjwt>=2.13.0",
    "pymongo>=4.18.0",
    "python-multipart>=0.0.32",
    "uvicorn[standard]>=0.32.0",
]

[project.optional-dependencies]
dev = [
    "httpx2>=2.0.0",
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "ruff>=0.8.0",
    "testcontainers>=4.15.0",
]

[project.scripts]
pesu-oauth2 = "src.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
markers = [
    "unit: Unit tests (no external services)",
    "integration: Integration tests (may require external services)",
    "atlas_smoke: Live Atlas X.509 smoke (opt-in via RUN_ATLAS_SMOKE=1)",
    "live_academy: Live PESU Academy login (opt-in via RUN_LIVE_ACADEMY=1)",
]

[tool.ruff]
target-version = "py313"
line-length = 120
exclude = [".venv", "__pycache__"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # pyflakes
    "I",    # isort
    "N",    # pep8-naming
    "W",    # pycodestyle warnings
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "C90",  # Complexity rules
    "ANN",  # Annotations rules
    "TYP",  # Type checking rules
    "Q000", # Quotation style rules
    "RET",  # Return type rules
]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["src"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.coverage.run]
source = ["src"]
branch = true
omit = ["src/__main__.py"]

[tool.coverage.report]
fail_under = 95
show_missing = true
skip_empty = true
